export const environment = {
  production: true,
  baseUrl: window.location.origin, // Base URL here
  apiUrl: 'https://api.gemini.technology/api' // global API URL (for production build)
  //... more of your variables
};
