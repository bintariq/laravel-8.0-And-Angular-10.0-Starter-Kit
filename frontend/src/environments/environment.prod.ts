export const environment = {
  production: true,
  baseUrl: window.location.origin, // Base URL here
  apiUrl: 'http://127.0.0.1:8000/api' // global API URL (for production build)
  //... more of your variables
};
